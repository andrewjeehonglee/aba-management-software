Fix demo mode: role toggle, role sync timing, session diagnostics

Fix 1: Remove isDemo header branch that replaced role toggle with CTA button.
Fix 2: Replace roleSettled gate with unconditional sync so owner role arriving
async from DB actually updates viewRole (was silently dropped on second effect run).
Fix 3: Add console.log to getSessionsToday showing UTC boundaries and row count.
Fix 4: TodaySessionsTile already uses clientId UUID correctly - no change needed.
